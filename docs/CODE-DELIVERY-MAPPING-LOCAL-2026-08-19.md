# Code Delivery Mapping — Local Snapshot — 2026-08-19

This document fills the frozen `docs/spec/v1-freeze-2026-08-18/05-CODE-DELIVERY-MAPPING-TEMPLATE.md` for the local working tree inspected on 2026-08-19. It is a local verification record, not an immutable release-candidate attestation.

**Overall result:** **Local/SQLite PASS** (`manage.py check`, migration-drift check, and 79/79 tests). **PostgreSQL NOT READY. Staging NOT READY. Restore exercise NOT READY. Production NOT READY.**

## Delivery identity

- Repository: local workspace `C:\Users\admin\Desktop\growth-os`
- Commit SHA: **NOT RECORDED** — the inspected tree is not bound to a committed candidate.
- Tag: **NOT RECORDED**.
- Branch: **NOT RECORDED**.
- Delivery timestamp and timezone: 2026-08-19 01:27 +08:00 (Asia/Shanghai), local working-tree snapshot.
- Backend language/framework/version: Python 3.12.13; Django 5.2.15.
- Frontend language/framework/version: server-rendered Django templates, HTML/CSS; no separate frontend framework or versioned frontend bundle.
- Database/version: SQLite 3.53.1 was tested locally. `compose.yaml` declares `postgres:17-alpine` as the target, but PostgreSQL was not exercised by this verification.
- Container/build tool: `Dockerfile` and Docker Compose (`compose.yaml`) are present; no container build was run for this mapping.
- Developer-reported completion: frozen V1 domain foundation, local Dogfood bootstrap, task/review/release UI, deterministic release gate, and manual publication-proof path are implemented in source.
- Independently verified completion: existing local virtual environment only — system check passed, no migration drift was detected, and 79 tests passed on SQLite. No exact-candidate, PostgreSQL, Staging, or restore verification was performed.

## Reproducibility

- Dependency lock files: `requirements.txt` pins direct Python package versions; `pyproject.toml` carries project metadata. There is no hash-locked transitive dependency artifact.
- Clean-environment setup command: `python -m venv .venv`, then `.venv\Scripts\python.exe -m pip install -r requirements.txt`. Declared here; not rerun in a clean environment during this verification.
- Build command: `docker compose build`. Declared; not run.
- Migration command: set `DATABASE_ENGINE=sqlite`, then `.venv\Scripts\python.exe manage.py migrate --noinput`. Test database migrations ran as part of the local suite.
- Seed command: `.venv\Scripts\python.exe manage.py bootstrap_dogfood`; a new Owner requires `BOOTSTRAP_OWNER_PASSWORD` in the process environment. `--full-demo` additionally requires separate environment passwords for any new human identities. The command creates no Task.
- Start command: `.venv\Scripts\python.exe manage.py runserver` for local SQLite, or `docker compose up --build` for the declared Compose/PostgreSQL topology.
- Health-check command and expected response: `Invoke-RestMethod http://127.0.0.1:8000/health/`; expected JSON is `{"status":"ok","database":"ok"}` from `core/views.py`.
- Test command: set `DATABASE_ENGINE=sqlite`, then `.venv\Scripts\python.exe manage.py test --verbosity 1`.
- Container image tag and digest: **NOT BUILT / NOT RECORDED**.

## Frozen entity mapping

Status is the entity's current frozen-V1 implementation status. `PASS` in this table means exercised on Local/SQLite only; it does not imply PostgreSQL or Staging acceptance.

| Group | Entity | Migration/table | Model/service | API/UI | Tests | Status | Gap/evidence |
|---|---|---|---|---|---|---|---|
| Identity | Principal | `accounts/migrations/0001_initial.py`, `0002_initial.py`, `0003_remove_permissiongrant_accounts_grant_explicit_scope_and_more.py`; `accounts_principal` | `accounts/models.py::Principal`; login gate in `accounts/backends.py`; role validation in `accounts/authorization.py` | `/accounts/login/`; `/admin/accounts/principal/` | `accounts/tests.py::PrincipalTests`; acting-role and inactive-principal negatives in `PermissionGrantTests` | PASS | Only persisted `ACTIVE` principals authenticate; local tests pass. |
| Identity | PermissionGrant | accounts migrations 0001–0003; `accounts_permissiongrant` | `accounts/models.py::PermissionGrant`; `resolve_authorization`, `require_authorization`, `validate_acting_role` in `accounts/authorization.py` | `/admin/accounts/permissiongrant/`; consumed by every mutating task/review/release action | `accounts/tests.py::PermissionGrantTests`; downstream authorization negatives | PASS | Hierarchical GLOBAL/PRODUCT/PLATFORM/ACCOUNT/SURFACE matching, most-specific ALLOW, and any-applicable DENY precedence are covered locally. |
| Product | Product | `products/migrations/0001_initial.py`; `products_product` | `products/models.py::Product`; `products/management/commands/bootstrap_dogfood.py` | `/admin/products/product/`; bootstrap command; task pages consume the bound Product | `products/tests.py`; `products/test_management.py` | PASS | Local bootstrap is idempotent and creates no Task. |
| Product | ProductProfileVersion | `products/migrations/0001_initial.py`; `products_productprofileversion` | `products/models.py::ProductProfileVersion.seal` | `/admin/products/productprofileversion/`; bootstrap command; displayed/consumed through Task contract | `products/tests.py::ProductProfileVersionTests`; bootstrap tests | PARTIAL | Seal manifest and instance-save immutability pass, but queryset/raw-SQL immutability and restricted PostgreSQL application-role permissions are not proven. |
| Task | Task | `workflow/migrations/0001_initial.py`; `workflow_task` | explicit state machine in `workflow/models.py::Task`; cross-domain guards in `workflow/services.py` | `/`, `/tasks/new/`, `/tasks/{task_id}/`, `/tasks/{task_id}/actions/{action}/` | `workflow/tests.py`; `dashboard/tests.py`; `core/test_acceptance.py` | PASS | New tasks are restricted to an ACTIVE product's current sealed profile and latest exact contract; creation replay, conflict, authorization, optimistic version and legal transitions pass locally. |
| Task | TaskContractVersion | `workflow/migrations/0001_initial.py`; `workflow_taskcontractversion` | `workflow/models.py::TaskContractVersion`; policy resolution in `releasegate/models.py` | bootstrap command; read through task detail and release gate; no standalone editor | `workflow/tests.py::test_exact_facts_drive_chain_from_draft_to_approved`; `releasegate/tests.py` exact-policy cases | PASS | Immutable exact ProfileVersion contract with separate DoR, DoD, and release criteria. |
| Task | TaskContractPolicyLink | `workflow/migrations/0001_initial.py`; `workflow_taskcontractpolicylink` | `workflow/models.py::TaskContractPolicyLink`; exact-policy resolver in `releasegate/models.py` | bootstrap command; consumed by release-gate action; no standalone editor | `releasegate/tests.py::test_gate_blocks_if_a_required_exact_contract_policy_is_missing_from_completed_run`; exact-version test | PASS | Unique exact ContractVersion/PolicyVersion links; fail-closed when required links/results are missing. |
| Task | TaskAssignment | `workflow/migrations/0001_initial.py`; `workflow_taskassignment` | append-only assignment operation in `workflow/models.py` | task action endpoint; assignment shown on task detail | `workflow/tests.py::test_assignment_is_explicit_idempotent_and_does_not_grant_review_or_publish`; `dashboard/tests.py::test_ready_assignment_is_explicit_and_only_assignee_can_start` | PASS | Explicit, scoped, idempotent assignment; assignment does not imply review/publish authorization. |
| Task | TaskStateEvent | `workflow/migrations/0001_initial.py`; `workflow_taskstateevent` | append-only event/state transition logic in `workflow/models.py`; prerequisites in `workflow/services.py` | generated by task/review/release UI actions and shown through task state | workflow transition/recovery tests; all five `core/test_acceptance.py` paths | PASS | `command_id`, `payload_hash`, expected/resulting version, sequence, and previous-event chain are present and tested. |
| Check | TaskCheckRun | `workflow/migrations/0001_initial.py`; `workflow_taskcheckrun` | `workflow/models.py::TaskCheckRun`; phase guard in `workflow/services.py::guard_check_run` | DoR/DoD task actions and task detail | `workflow/tests.py` DoR/DoD/immutability cases; dashboard DoR/upload/rework cases | PASS | DoR and DoD are separate immutable attempts with expected/actual counts and exact ContractVersion. |
| Check | TaskCheckResult | `workflow/migrations/0001_initial.py`; `workflow_taskcheckresult` | `workflow/models.py::TaskCheckResult` | recorded/displayed with DoR/DoD run through task actions/detail | `workflow/tests.py::test_completed_check_facts_are_immutable`; AC-01/02/03 | PASS | Unique criterion key/sequence per run and immutable historical failures. |
| Submission | TaskSubmission | `contentops/migrations/0001_initial.py` + `0002_initial.py`; `contentops_tasksubmission` | `contentops/models.py::TaskSubmission`; submission guards in `workflow/services.py` | upload/submission task action and task detail | `contentops/tests.py`; dashboard upload/rework tests; AC-01/03 | PASS | Exact latest passing DoD, primary AssetVersion, task version, manifest, supersession, and triggering review are enforced. |
| Submission | TaskSubmissionAssetLink | contentops migrations 0001–0002; `contentops_tasksubmissionassetlink` | `contentops/models.py::TaskSubmissionAssetLink` | created by upload/submission action; represented in task detail | `contentops/tests.py::test_submission_has_one_exact_primary_and_rejects_incomplete_dod`; AC-03 | PASS | Unique asset link and one conditional PRIMARY link per submission. |
| Review | ReviewDecision | contentops migrations 0001–0002; `contentops_reviewdecision` | `contentops/models.py::ReviewDecision`; `workflow/services.py::guard_review` | `/review/`, `/review/{task_id}/`, `/review/{task_id}/action/` | `contentops/tests.py`; `dashboard/test_review_release.py`; AC-01/03/05 | PASS | One immutable final human decision per exact Submission; independent REVIEW and EDIT grants are tested. |
| Asset | ContentAsset | contentops migrations 0001–0002; `contentops_contentasset` | `contentops/models.py::ContentAsset` | task upload action/detail; no standalone library in frozen V1 | `contentops/tests.py::test_uuidv7_and_asset_version_are_append_only_and_idempotent`; dashboard upload tests | PASS | Append-only UUIDv7 asset identity with idempotent creation. Searchable Content Library is post-closure backlog. |
| Asset | ContentAssetVersion | contentops migrations 0001–0002; `contentops_contentassetversion` | `contentops/models.py::ContentAssetVersion` | task upload action/detail; exact version appears in submission/release detail | contentops append-only/idempotency and rework tests; dashboard upload/rework tests | PASS | Version, content hash, manifest hash, and exact primary binding are preserved. |
| Policy | PolicyDefinition | `releasegate/migrations/0001_initial.py` + `0002_initial.py`; `releasegate_policydefinition` | `releasegate/models.py::PolicyDefinition`; bootstrap command | bootstrap configuration; consumed by release gate; no frozen-V1 policy editor | `releasegate/tests.py`; `products/test_management.py` | PASS | Runtime definition is implemented; dynamic policy monitoring/editor is post-closure scope. |
| Policy | PolicyVersion | releasegate migrations 0001–0002; `releasegate_policyversion` | `releasegate/models.py::PolicyVersion`; exact set/hash resolution in `releasegate/models.py` | bootstrap configuration; consumed by gate action/detail | releasegate missing/exact/current-policy tests; bootstrap tests | PASS | Immutable exact policy version and policy-set hash; current version cannot replace the Contract's exact version. |
| Policy | RuleEvaluationRun | releasegate migrations 0001–0002 and `0004_alter_publication_requested_by_acting_role_and_more.py`; `releasegate_ruleevaluationrun` | `releasegate/models.py::RuleEvaluationRun`; `releasegate/services.py::orchestrate_v1_release_gate` | `/release/{task_id}/gate/`; displayed on release detail | releasegate deterministic orchestration, evaluator, rollback, and replay tests | PASS | Complete-count, exact policy set/context, independent evaluator, command id, and payload hash are tested. |
| Policy | RuleEvaluationResult | releasegate migrations 0001–0002; `releasegate_ruleevaluationresult` | `releasegate/models.py::RuleEvaluationResult`; release orchestration service | generated by gate action and displayed through gate result | releasegate fail-closed, immutability, replay, missing-rule/policy tests | PASS | Immutable result per run/policy/rule with exact evidence fields. |
| Channel | ChannelAccount | releasegate migrations 0001–0002; `releasegate_channelaccount` | `releasegate/models.py::ChannelAccount`; bootstrap command | bootstrap configuration; selected by release-gate form/action | releasegate context tests; bootstrap tests; dashboard release tests | PASS | Unique platform/external account reference; general account-management UI is outside the minimum loop. |
| Channel | RuntimeEnvironment | releasegate migrations 0001–0002; `releasegate_runtimeenvironment` | `releasegate/models.py::RuntimeEnvironment`; bootstrap command | bootstrap configuration; selected/consumed by release gate | releasegate environment/context tests; bootstrap tests | PASS | Exact environment is bound into the gate; PostgreSQL/Staging execution remains unverified. |
| Channel | AccountEnvironmentBinding | releasegate migrations 0001–0002; `releasegate_accountenvironmentbinding` | `releasegate/models.py::AccountEnvironmentBinding`; current-context resolver in `releasegate/services.py` | bootstrap configuration; consumed by gate/proof actions | releasegate context-change, stale-gate, and V1 orchestration tests | PASS | Immutable, versioned, time-bounded exact account/environment binding. |
| Channel | CapabilityState | releasegate migrations 0001–0002; `releasegate_capabilitystate` | `releasegate/models.py::CapabilityState`; current-context resolver in `releasegate/services.py` | bootstrap creates OPEN manual-publish capability; gate/proof actions recheck it | releasegate closed-capability, stale-context, and orchestration tests | PASS | Immutable versioned capability; CLOSED blocks and recovery requires a new gate. |
| Gate | ReleaseGateRecord | releasegate migrations 0001–0004; `releasegate_releasegaterecord` | `releasegate/models.py::ReleaseGateRecord`; `releasegate/services.py::orchestrate_v1_release_gate` | `/release/{task_id}/`, `/release/{task_id}/gate/` | `releasegate/tests.py`; `dashboard/test_review_release.py`; AC-01/03/04/05 | PASS | Exact Submission/Review/Asset/Contract/publisher/grant/account/environment/binding/capability/evaluation references and context hash are enforced. |
| Gate | ReleaseGateEvaluationLink | releasegate migrations 0001–0002; `releasegate_releasegateevaluationlink` | `releasegate/models.py::ReleaseGateEvaluationLink`; release orchestration service | generated by gate action; summarized in release detail | releasegate complete-link/missing-policy/immutability tests; AC-01/04 | PASS | One immutable exact result link; incomplete result sets fail closed. |
| Publication | Publication | releasegate migrations 0001–0002 and 0004; `releasegate_publication` | `releasegate/models.py::Publication`; release orchestration and manual-proof services | `/release/`, `/release/{task_id}/`, gate/proof actions | releasegate publication/orchestration tests; dashboard release tests; AC-01/04 | PASS | Created as `GATE_PENDING`; the system records intent and never performs external publishing automatically. |
| Publication | PublicationEvent | releasegate migrations 0001–0002 and 0004; `releasegate_publicationevent` | `releasegate/models.py::PublicationEvent`; `releasegate/services.py::record_manual_publication_proof` | `/release/{task_id}/proof/`, `/release/{task_id}/done/`; event history on release detail | releasegate manual path, stale gate, immutable event, proof replay tests; AC-01/04/05 | PASS | Ordered immutable READY and MANUAL_PUBLISHED proof events with command/payload/version/actor/grant metadata. |

Framework-owned Django authentication groups/permissions, admin/session/content-type tables, and `django_migrations` are technical infrastructure, not additional business entities. They are created by Django migrations; login is session-based, access is gated by Principal status plus the central authorization resolver, and no claim is made here that Staging cookie, proxy, database-role, or log configuration has been verified.

## Required invariant evidence

| Invariant | Database evidence | Service/API evidence | Negative test | Status |
|---|---|---|---|---|
| ProductProfileVersion is sealed and immutable | `products_unique_profile_version` and `products_profile_complete_seal` constraints; manifest/seal columns | `ProductProfileVersion.seal` and instance `save` guard in `products/models.py` | `products/tests.py::test_seal_records_manifest_and_blocks_edits` | PARTIAL — local instance mutation is blocked, but queryset/raw SQL and restricted PostgreSQL application-role enforcement are not proven. |
| TaskContractVersion binds exact profile and policy versions | PROTECT FKs plus `workflow_unique_contract_version` and `workflow_unique_contract_policy_link` | contract manifest in `workflow/models.py`; exact policy resolution and gate orchestration in `releasegate/models.py` and `releasegate/services.py` | releasegate missing-policy and exact-version tests | PASS — Local/SQLite. |
| ReleaseGate binds exact Submission, ReviewDecision, AssetVersion, and Publication | non-null PROTECT FKs in releasegate migrations; one exact evaluation run and result links | `ReleaseGateRecord` validation/manager plus `orchestrate_v1_release_gate` | old-chain, wrong-policy, missing-result, stale-context, and unauthorized tests in `releasegate/tests.py` | PASS — Local/SQLite. |
| Publication is a release intent until a valid Gate passes | Publication starts `GATE_PENDING`; gate/event FKs and ordered event constraints | Publication manager and release service only project READY after a passing current gate | `test_publication_starts_gate_pending_and_manual_path_is_event_only`; closed/nonpassing tests | PASS — Local/SQLite. |
| Manual publish rechecks current Grant, Capability, Environment, and mandatory policies | gate stores exact context FKs and hashes; each proof is a new PublicationEvent | `record_manual_publication_proof` recomputes current context and authorization | last-mile recompute, revoked/closed/stale context, hierarchical DENY, and invalid-proof tests | PASS — Local/SQLite. |
| State transitions use command id, sequence, and expected version | unique command IDs, unique per-task/per-publication sequence, and `resulting = expected + 1` constraints | Task and Publication event managers lock/check current version and replay payload | workflow transition conflict/replay tests; AC-05 | PASS — Local/SQLite. |
| Append-only facts cannot be updated or deleted by application roles | PROTECT FKs and uniqueness/check constraints; no PostgreSQL role/trigger evidence | append-only QuerySet/model guards in workflow, contentops, and releasegate | immutability tests in `workflow/tests.py`, `contentops/tests.py`, and `releasegate/tests.py` | PARTIAL — frozen facts are guarded through the ORM, but ProductProfileVersion has the noted queryset gap and database-role enforcement is unverified. |
| Unauthorized or stale actions fail closed | transactions and uniqueness/version constraints prevent partial commits | central `accounts/authorization.py` plus state/context guards | accounts DENY/role/status tests; dashboard forbidden-action tests; releasegate rollback tests; AC-05 | PASS — Local/SQLite. |
| Repeated commands do not create duplicate side effects | unique command IDs and entity/version/link constraints | managers compare canonical payload hashes and return the original fact only for an exact replay | replay/conflicting-payload tests across workflow, contentops, releasegate, dashboard, and AC-05 | PASS — Local/SQLite. |
| Secrets are absent from source, database plaintext, responses, and logs | password hashes use Django auth; no plaintext-secret business field is mapped | bootstrap accepts new human passwords only from distinct environment variables; health response contains no secrets | `products/test_management.py` password-source/distinctness/rollback tests | PARTIAL — source behavior is covered, but no retained whole-tree secret-scan report, PostgreSQL inspection, response crawl, or Staging log review exists. |

## Five-path mapping

The queries below are reproducible evidence queries for a selected test fixture. The test suite asserts equivalent ORM state, but this mapping does not claim a retained SQL export, screenshot, or Staging audit log.

| Path | Test IDs | UI/API evidence | Database/audit evidence | Result | Open defect |
|---|---|---|---|---|---|
| AC-01 Normal manual-publishing path | `core.test_acceptance.FrozenV1AcceptanceTests.test_ac01_normal_manual_publication_has_one_traceable_chain`; `dashboard.test_review_release.ReviewReleaseUISliceTests.test_release_gate_proof_and_owner_completion_are_manual_and_idempotent`; releasegate V1 orchestration test | Task actions at `/tasks/{id}/actions/{action}/`; review action; `/release/{id}/gate/`, `/proof/`, and `/done/` | Repro query: `SELECT event_sequence, from_state, to_state FROM workflow_taskstateevent WHERE task_id=? ORDER BY event_sequence`; and `SELECT event_sequence, event_type, release_gate_id FROM releasegate_publicationevent WHERE publication_id=? ORDER BY event_sequence`. Exact FK chain is asserted by AC-01. No retained screenshot/log. | PASS — Local/SQLite | Repeat with exact immutable candidate on PostgreSQL/Staging and retain IDs, SQL output, logs, and manual proof. |
| AC-02 DoR/input blocked path | `core.test_acceptance.FrozenV1AcceptanceTests.test_ac02_dor_blocked_requires_new_run_and_recorded_recovery`; `dashboard.tests.ControlledTaskUiTests.test_blocked_dor_is_preserved_before_a_new_pass_can_make_task_ready` | DoR and block/recovery actions through task detail/action UI | Repro query: `SELECT attempt_number, status, aggregate_result, expected_criterion_count, actual_criterion_count FROM workflow_taskcheckrun WHERE task_id=? AND check_kind='DOR' ORDER BY attempt_number`; event-chain query above. Tests assert no downstream Submission/Gate/Publication. No retained screenshot/log. | PASS — Local/SQLite | PostgreSQL/Staging evidence not run or retained. |
| AC-03 Review rework path | `core.test_acceptance.FrozenV1AcceptanceTests.test_ac03_rework_creates_new_facts_and_old_chain_cannot_gate`; `dashboard.tests.ControlledTaskUiTests.test_human_rework_resumes_and_creates_new_version_dod_and_submission` | `/review/{id}/action/` records CHANGES_REQUESTED; task UI resumes, uploads v2, runs new DoD, and resubmits | Repro query: `SELECT submission_number, primary_asset_version_id, supersedes_submission_id, triggering_review_id FROM contentops_tasksubmission WHERE task_id=? ORDER BY submission_number`; AC-03 asserts both decisions and rejects the old gate chain. No retained screenshot/log. | PASS — Local/SQLite | PostgreSQL/Staging evidence not run or retained. |
| AC-04 Release Gate blocked/stale path | `core.test_acceptance.FrozenV1AcceptanceTests.test_ac04_blocked_gate_is_immutable_and_recovery_requires_new_gate`; releasegate closed-capability, stale-context, and old-gate tests | `/release/{id}/gate/` creates a blocked immutable gate; later gate/proof actions require restored current context | Repro query: `SELECT id, outcome, context_sha256, failure_reasons, evaluation_run_id FROM releasegate_releasegaterecord WHERE publication_id=? ORDER BY evaluated_at`; PublicationEvent query proves no premature READY/published event. No retained screenshot/log. | PASS — Local/SQLite | PostgreSQL/Staging context-change run and retained audit extract are missing. |
| AC-05 Idempotency/authorization/conflict recovery path | `core.test_acceptance.FrozenV1AcceptanceTests.test_ac05_replay_authorization_and_optimistic_conflict_leave_no_partial_facts`; workflow transition replay/conflict test; contentops/releasegate/dashboard replay and unauthorized rollback tests; accounts DENY/acting-role/status tests | Repeated/conflicting task, review, gate, and proof posts are exercised; forbidden requests return 403 and tests assert zero partial business facts | Repro query: `SELECT command_id, COUNT(*) FROM workflow_taskstateevent GROUP BY command_id HAVING COUNT(*)>1`; and `SELECT task_id, event_sequence, COUNT(*) FROM workflow_taskstateevent GROUP BY task_id, event_sequence HAVING COUNT(*)>1`. Tests assert no orphan checks, reviews, gates, or events. No retained structured security log. | PASS — Local/SQLite | SQLite cannot prove PostgreSQL row-lock/concurrency behavior; structured rejection audit evidence is also missing. |

## Delivery gaps

### P0

- **Release identity/evidence blocker:** there is no recorded commit SHA, tag, immutable container tag, or image digest for this working-tree snapshot. It cannot be promoted as the exact frozen candidate.
- **Database/Staging blocker:** all reported passes are SQLite-local. PostgreSQL migrations, locking/concurrency behavior, full five-path execution, proxy/TLS settings, and exact-candidate Staging smoke tests are **NOT READY**.
- **Recovery blocker:** no backup/restore rehearsal demonstrates the frozen RPO (no greater than 1 hour) and RTO (no greater than 4 hours). Restore is **NOT READY**.
- No known failing Local/SQLite P0 test was observed; these are production-release evidence gates, not claims of a local test failure.

### P1

- Harden sealed `ProductProfileVersion` against queryset updates and prove append-only enforcement for the restricted PostgreSQL application role; current instance-save coverage is insufficient as database evidence.
- Add and verify structured, secret-safe rejection/audit logging for authorization, idempotency-payload conflict, stale version, and gate-context failures. Current tests prove no partial business fact but do not retain the acceptance matrix's security-log evidence.
- Retain an exact-candidate evidence pack: UI screenshots, request/response identifiers, SQL/audit extracts, command output, executor, and reviewer. Current review/release routes and tests exist, but no human browser evidence pack is recorded.
- Re-run clean dependency installation and Docker image build from an immutable candidate; current verification reused the existing local virtual environment.

### P2 / Backlog

- Keep the items in `docs/spec/v1-freeze-2026-08-18/03-POST-CLOSURE-BACKLOG.md` outside the Runtime until closure: expanded product/evidence context, external-demand import, opportunity/initiative/channel planning, expanded role views and Content Library, Issue workflow, performance/SEO/GEO feedback, Human Learning, commerce/process telemetry, dynamic policy monitoring, and connectors.
- Do not add those entities merely to improve this mapping; their current status is `OUT_OF_SCOPE` rather than missing from the frozen 28-entity Runtime.

### Blueprint code that must be removed or disabled

- No automatic external publishing is mapped: `Publication` remains an intent, a human performs the platform action, and the system only records proof.
- Automatic rule approval/relaxation, advertising/budget/account creation, full meeting governance, automated Replay/Shadow/Canary/Activation, connector rollout, complex attribution, multi-primary assignment/dependency automation, multi-model routing, multitenancy, billing, and agency SaaS remain disabled/out of scope per `docs/spec/v1-freeze-2026-08-18/03-POST-CLOSURE-BACKLOG.md`.
- Static model inventory found no additional Blueprint business entity among the mapped apps. This is a source-inventory statement, not a deployed-runtime attestation.

## Verification outcome

- Clean build: **NOT READY** — Local system/migration checks pass in the existing virtual environment; a clean dependency install and container build were not run.
- Migration and seed: **LOCAL/SQLITE PASS** — test database migrations applied, migration-drift check reported no changes, and bootstrap command tests passed; **POSTGRESQL NOT READY**.
- Minimum vertical loop: **LOCAL/SQLITE AUTOMATED UI SLICES PASS / HUMAN DOGFOOD EVIDENCE PENDING** — task creation, task execution, review, release-gate, manual-proof and completion routes are wired and tested; no retained human-browser or Staging evidence exists.
- Five paths: **LOCAL/SQLITE PASS** — AC-01 through AC-05 passed in the 79-test suite; **POSTGRESQL/STAGING NOT READY**.
- Security and permission negatives: **LOCAL/SQLITE PASS** for Principal status, acting role, hierarchical DENY precedence, scoped authorization, stale/conflicting commands, rollback, and no-partial-fact assertions; structured Staging audit-log evidence is **NOT READY**.
- Staging deployment: **NOT READY**.
- Restore exercise: **NOT READY**.
- Production decision: **NOT READY / NO-GO** until the P0 release-evidence gates above are closed against one immutable candidate.

## Local verification record

- `.venv\Scripts\python.exe manage.py check` → `System check identified no issues (0 silenced).`
- `.venv\Scripts\python.exe manage.py makemigrations --check --dry-run` → `No changes detected`.
- `.venv\Scripts\python.exe manage.py test --verbosity 1` → `Ran 79 tests ... OK` on SQLite.
